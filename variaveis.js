let none = "pedro"; //Variável que armazena uma String
const idade = 15; //Variável que armazena um numero
var  cidade = "São Paulo"; //Variável que armazena um String

// (const) = Variável que não é possivel mudar o valor dela no meio do codigo

// (var) =Variável que é possivel mudar o valor dela no meio do codigo

// (let) = (var) porém mais atualizado, por conta que o var vai ser descontinuado

  // mostrar a variável nome no terminal
console. log(nome);

// Mostrar uma frase com todas as variáveis criadas
console.log("o meu nome é" + nome + ", eu tenho"+ idade + " anos " + " e eu nasci em " + cidade)

console.log(`O meu nome é ${nome}, eu tenho ${idade} anos e eu nasci em ${cidade}`);

let estaChovendo = false; // variável Boolean que indica que esta falso o valor 
let taNublado = true; // variável Boolean que indica que esta verdadeiro o valor

// Variáveis Undefined
let marca;
console.log(marca); // undefined

// Variáveis Null
let endereco = null;

// tipo primitivo da Variável
// typeof
console.log(typeof nome);